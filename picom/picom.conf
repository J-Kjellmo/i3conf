backend = "xrender";  # Use xrender as backend

# Set the opacity for active and inactive windows
inactive-opacity = 100;  # Set inactive windows to fully opaque
active-opacity = 100;    # Set active windows to fully opaque

# Enable other necessary settings
shadow = false;
fading = false;
transparency = false;
